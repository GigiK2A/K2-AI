fastapi>=0.115
uvicorn[standard]>=0.32
# MCP client — il backend interroga i MCP server di Luca via stdio (lib/mcp_client.py).
mcp>=1.2.0
# I MCP server di Luca eseguiti DENTRO il backend (architettura A2, "la sua roba").
# DISATTIVATI nel build di deploy finché non sono verificati su Railway (build da
# sorgente, env diverso; norme ha gia il wheel rotto): un fallimento qui romperebbe
# TUTTO il deploy. Il codice (lib/mcp_client.py) degrada a "non disponibile" senza
# rompere. Per attivarli in prod: pinnare a un tag, decommentare, watchare il build.
# k2a-quant @ git+https://github.com/inglucarossi73/k2a-mcp-quant@<tag>
# k2a-agevolazioni @ git+https://github.com/inglucarossi73/k2a-mcp-agevolazioni@<tag>
# k2a-elettrico @ git+https://github.com/inglucarossi73/k2a-mcp-elettrico@<tag>
# vs-strutturale @ git+https://github.com/inglucarossi73/k2a-mcp-strutturale@<tag>
# k2a-norme-tecniche @ git+...@<tag>   (wheel build rotto lato repo → fallback vendorizzato)
pydantic[email]>=2.10
python-dotenv>=1.0
PyJWT[crypto]>=2.10
supabase>=2.30
anthropic>=0.40
stripe>=11.4
resend>=2.5
httpx>=0.27
pdfplumber>=0.11
# BM25 retrieval per RAG su file caricati (MVP, no vector DB).
rank-bm25>=0.2.2
reportlab>=4.2
Jinja2>=3.1
python-docx>=1.1
# Excel (.xlsx) export del deliverable — tabelle/calendari editoriali.
openpyxl>=3.1
pytest>=8.0
pytest-mock>=3.14
slowapi>=0.1.9
# Optional — initialized only if SENTRY_DSN is set at runtime.
sentry-sdk[fastapi]>=2.0
# Optional — analytics; no-op if POSTHOG_API_KEY is not set.
posthog>=3.7

# Production (Railway): point at the FastAPI backend deployed at board-api.k2-ai.it
NEXT_PUBLIC_API_BASE_URL=https://board-api.k2-ai.it

# PostHog analytics — server-side only (NEVER exposed to client / browser)
POSTHOG_PERSONAL_API_KEY=
POSTHOG_PROJECT_ID=179781
POSTHOG_HOST=https://eu.i.posthog.com

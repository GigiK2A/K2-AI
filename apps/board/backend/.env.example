SUPABASE_URL=https://uiuvwzrmrdqbfajguuab.supabase.co
SUPABASE_SERVICE_KEY=
BOARD_USERNAME=luigi
# bcrypt hash — generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpw', bcrypt.gensalt()).decode())"
BOARD_PASSWORD_HASH=
# 32+ random bytes — generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
BOARD_SESSION_SECRET=
BOARD_CORS_ORIGINS=https://board.k2-ai.it,http://localhost:3000,http://localhost:3001
SENTRY_DSN=
ANTHROPIC_API_KEY=
# Stripe webhook signing secret — required to activate /api/webhooks/stripe.
# Obtain from Stripe dashboard -> Developers -> Webhooks -> reveal signing secret.
STRIPE_WEBHOOK_SECRET=whsec_...
# Google Calendar — stubs for future sync (Sprint 9+). Leave empty for now.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
LOG_FORMAT=json
PORT=8000
# PostHog analytics (consumed by the frontend Next.js server, kept here for reference) — server-side only
POSTHOG_PERSONAL_API_KEY=
POSTHOG_PROJECT_ID=179781
POSTHOG_HOST=https://eu.i.posthog.com

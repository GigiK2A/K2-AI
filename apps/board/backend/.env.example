SUPABASE_URL=https://uiuvwzrmrdqbfajguuab.supabase.co
SUPABASE_SERVICE_KEY=
BOARD_USERNAME=luigi
# bcrypt hash — generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpw', bcrypt.gensalt()).decode())"
BOARD_PASSWORD_HASH=
# 32+ random bytes — generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
BOARD_SESSION_SECRET=
BOARD_CORS_ORIGINS=https://board.k2-ai.it,http://localhost:3000,http://localhost:3001
SENTRY_DSN=
ANTHROPIC_API_KEY=
LOG_FORMAT=json
PORT=8000
